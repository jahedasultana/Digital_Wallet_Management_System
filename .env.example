# Server Port
PORT=5000

# MongoDB Connection URI
DB_URL=mongodb+srv://<username>:<password>@cluster0.9rpk71q.mongodb.net/<dbname>?retryWrites=true&w=majority&appName=Cluster0

# Node Environment
NODE_ENV=development

# Frontend URL (for CORS, redirects, etc.)
FRONTEND_URL=http://localhost:3000

# Cloudinary Configuration
CLOUDINARY_CLOUD_NAME=<your-cloudinary-cloud-name>
CLOUDINARY_API_KEY=<your-cloudinary-api-key>
CLOUDINARY_API_SECRET=<your-cloudinary-api-secret>

# JWT Settings
JWT_ACCESS_SECRET=<your-jwt-access-secret>
JWT_REFRESH_SECRET=<your-jwt-refresh-secret>
JWT_ACCESS_EXPIRES=1d
JWT_REFRESH_EXPIRES=30d

# Bcrypt Salt Round (for password hashing)
BCRYPT_SALT_ROUND=10

# Admin Credentials (use dummy/example values)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=supersecurepassword
ADMIN_PHONE=01800000000

# SMTP Email Configuration (for sending emails)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-email@example.com
SMTP_PASS=your-email-password-or-app-password
SMTP_FROM=your-email@example.com
